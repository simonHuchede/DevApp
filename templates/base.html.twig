<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}DevApp{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
    {% block entete %}
        <nav>
            <a href="{{ path('main_accueil') }}">Accueil</a>
            {% if not app.user %}
                <button><a href="{{ path('app_login') }}">Se connecter</a></button>
                <button><a href="{{ path('app_register') }}">S'enregistrer</a></button>
            {% endif %}

            {% if app.user %}
                <div><button><a href="{{ path('app_logout') }}">Se déconnecter</a></button>{{ app.user.pseudo }}</div>
            {% endif %}

        </nav>
    {% endblock %}
        {% block body %}{% endblock %}
    {% block footer %}
    {% endblock %}

    </body>
</html>
